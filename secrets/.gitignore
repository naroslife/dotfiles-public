# Ignore all files in secrets directory by default
*

# Allow .gitignore itself
!.gitignore

# Allow YAML files (these will be encrypted with sops)
!*.yaml

# Never commit private keys
**/*.key
**/*.txt
